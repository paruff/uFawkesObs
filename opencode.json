{
  "$schema": "https://opencode.ai/config.json",
  "$comment": "Static fallback config — the opencode.yml workflow overwrites this at runtime with the correct proxy URL and key availability. Commit this file as a safe default (no proxy, Gemini primary).",
  "models": [
    {
      "$comment": "NVIDIA NIM primary — only active when nim-proxy is running",
      "name": "nvidia-primary",
      "provider": "openai",
      "model": "nvidia/qwen3-coder-480b-a35b-instruct",
      "baseURL": "http://localhost:8000/v1",
      "apiKey": "proxy-auth"
    },
    {
      "$comment": "NVIDIA NIM secondary — larger reasoning model, same proxy",
      "name": "nvidia-secondary",
      "provider": "openai",
      "model": "nvidia/nemotron-3-ultra-550b-a55b",
      "baseURL": "http://localhost:8000/v1",
      "apiKey": "proxy-auth"
    },
    {
      "$comment": "Google Gemini — fast, generous free quota; env var: GOOGLE_API_KEY",
      "name": "gemini-fallback",
      "provider": "google",
      "model": "google/gemini-2.5-flash"
    },
    {
      "$comment": "OpenCode Zen free tier — last resort; env var: OPENCODE_API_KEY",
      "name": "opencode-zen-fallback",
      "provider": "openai",
      "model": "opencode-zen/deepseek-v4-flash-free",
      "baseURL": "https://api.opencode.ai/v1"
    }
  ],
  "fallback": {
    "enabled": true,
    "on": ["rate_limit", "timeout", "server_error"],
    "order": [
      "nvidia-primary",
      "nvidia-secondary",
      "gemini-fallback",
      "opencode-zen-fallback"
    ]
  },
  "log": {
    "level": "info"
  }
}
