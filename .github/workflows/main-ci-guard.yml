name: Main CI Guard

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  actions: read

jobs:
  check-main-ci:
    name: "🛡️ Main CI Health"
    uses: paruff/ufawkespipe/.github/workflows/reusable-main-ci-guard.yml@0d3addbf3371b817ac5c2ce68247d564b01cbd70 # v1.2.0
    with:
      workflow-id: ci-pipeline.yml
      workflow-name: "Pre-Merge Pipeline"

  check-acceptance-full:
    name: "🛡️ Acceptance Full Health"
    uses: paruff/ufawkespipe/.github/workflows/reusable-main-ci-guard.yml@0d3addbf3371b817ac5c2ce68247d564b01cbd70 # v1.2.0
    with:
      workflow-id: ci-acceptance-full.yml
      workflow-name: "Acceptance Full (Post-Merge)"
