# Unit Testing Requirements
# Python dependencies for configuration validation tests
#
# This file expresses intent (loose ranges). `make test-unit` and CI actually
# install from requirements.lock.txt (exact pins) for determinism — edit this
# file, then regenerate the lock per the header comment in
# requirements.lock.txt.

pytest>=7.4.3
pytest-asyncio>=1.4.0
PyYAML>=6.0.1
jsonschema>=4.20.0
yamllint>=1.33.0
aiohttp>=3.14.3
aiosqlite>=0.20
fastapi>=0.141.1
pytest-bdd>=8.0
requests>=2.31
