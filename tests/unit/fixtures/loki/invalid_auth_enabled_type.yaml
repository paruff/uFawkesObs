# Invalid: auth_enabled is not a boolean
# This should fail validation

auth_enabled: "false" # Should be boolean, not string!

server:
  http_listen_port: 3100

schema_config:
  configs:
    - from: 2024-01-01
      store: tsdb
      object_store: filesystem
      schema: v13
