# Invalid: Invalid port number
# This should fail validation

server:
  http_listen_port: 99999 # Port out of range!

distributor:
  receivers:
    otlp:
      protocols:
        grpc:
          endpoint: "0.0.0.0:4317"

ingester:
  max_block_duration: 5m

storage:
  trace:
    backend: local
    local:
      path: /var/tempo
